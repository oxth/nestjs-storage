# API Reference

This section covers `StorageService`'s methods and the public interfaces/types. For a machine-readable, exhaustive version of everything on this site (plus internal driver behavior notes), see [`llm-full.md`](https://github.com/oxth/nestjs-storage/blob/main/llm-full.md) in the repo — it's kept in sync with the source and is the most complete reference available.

- [StorageService](/api/storage-service) — every method, grouped by purpose
- [Interfaces & Types](/api/interfaces) — `StorageModuleOptions`, `DiskOptions`, `StoredFile`, `FakeDisk`, `LocalDriver`, `FileExtensionValidator`, and the exported helper functions

For everything else — module registration, upload interceptors, naming strategies, signed URLs, testing — see the [Guide](/guide/).
